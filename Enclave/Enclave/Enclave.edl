enclave {
    from "sgx_tstdc.edl" import *;

    trusted {
        /* define ECALLs here. */
		public unsigned int e_generate_random ([out, size=1] unsigned char *result);
    };

    untrusted {
        /* define OCALLs here. */

    };
};
